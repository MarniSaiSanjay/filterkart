{
  "name": "filterkart",
  "version": "0.1.0",
  "description": "Save and reapply shopping-site filter presets with one click.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/check.js",
    "package": "node scripts/package.mjs",
    "test": "node test/index.js",
    "verify": "npm run build && npm test",
    "e2e": "node scripts/e2e-check.js",
    "icons": "node scripts/gen-icons.cjs"
  }
}
